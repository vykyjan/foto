<h1>Gallery Photos:	<%= @gallery.name %></h1>
<br />

<p>Pokud chcete prohlížet fotografie, stačí kliknout na miniatury<p>

<br />
<%= link_to 'Zpět', gallery_path(@gallery) %> |
<%= link_to 'Nahrát foto', new_gallery_photo_path(@gallery) %>
<br />
<div id="gallery">
  <ul id="grid">
    <% @photos.each do |pic| %>
     <li><%= link_to image_tag(pic.photo_file.url(:thumb).to_s), gallery_photo_path(@gallery,pic) %></li>
    <% end %>
  </ul>
</div>

    