<h1>Všechny galerie</h1>

<table>
  <tr>
    <th>Jméno</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<br>
<% @galleries.each do |gallery| %>
  <tr>
    <td><strong><%= link_to gallery.name, gallery_path(gallery.id) %><strong></td>
    <td><%= link_to 'Změnit', edit_gallery_path(gallery) %></td>
    <td><%= link_to 'Smazat', gallery, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

</br>

<%= link_to 'Nová galerie', new_gallery_path,  class: "btn btn-large btn-primary" %>
 

